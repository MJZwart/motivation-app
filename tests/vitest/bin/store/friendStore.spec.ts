// import {createPinia, setActivePinia} from 'pinia';
// import {useFriendStore} from '/js/store/friendStore';
// import axios from 'axios';

// * Choosing not to test it as most of it would be mocked and hardly tests anything at all

// vi.mock('axios');

// describe('Friend store' , () => {
//     beforeEach(() => {
//         setActivePinia(createPinia());
//     });
//     it('sends a friend request', async() => {
//         // Arrange
//         vi.spyOn(axios, 'post').mockImplementation(() => Promise.resolve());
//         const friendId = '1';
//         const friendStore = useFriendStore();
//         // Act
//         await friendStore.sendRequest(friendId);
//         // Assert
//         expect(axios.post).toHaveBeenCalledOnce();
//     });
//     it('gets all friend requests', async() => {
//         // Arrange
//         vi.spyOn(axios, 'get').mockImplementation(() => Promise.resolve({'data': 'data'}));
//         const friendStore = useFriendStore();
//         // Act
//         await friendStore.getRequests();
//         // Assert
//         expect(friendStore.requests).toEqual('data');
//     });
//     // it('gets all friends', async() => {
//     //     // Arrange
//     //     vi.spyOn(axios, 'get').mockImplementation(() => Promise.resolve({'data': 'data'}));
//     //     const friendStore = useFriendStore();
//     //     // Act
//     //     await friendStore.getRequests();
//     //     // Assert
//     //     expect(friendStore.requests).toEqual('data');
//     // });
// });

it('', async() => {
    // Arrange

    // Act

    // Assert
});