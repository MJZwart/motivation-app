## NOTE
Characters have been removed from the site. The only current option for rewards is to choose a village, or no rewards. This gives me the opportunity to move forward with the gamification ideas I had without being held back by needlessly complex code to account for two options.

#### Features

-   Added an option to create a banner on the top of the screen for important notices, such as upcoming maintenance or breaking changes.

#### Bugs

-   Fixed an issue with cookies being duplicated until the server stops accepting calls. This was due to a combination of Laravel updates and old code. The whole API will needs looking into, because that part is very old by now.
-   Fixed an issue of confirmation modals not closing
-   Fixed an issue of multiple starter-tasks being created into multiple task lists, rather than being in a single task list, when creating a new account. Apparently I only ever tested with adding a single starter task?

#### Code

-   Updated many packages in both front- and back-end.
-   Removed Pinia and made my own (very basic) idea to hold data, to reduce overhead and have a more logical approach to data. This leads to more options with regards to improving the data usage in the front-end. Will need work to be more efficient.

#### Known bugs

-   Honestly with all the big changes I'd be surprised if everything still works flawlessly.